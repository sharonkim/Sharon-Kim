function rotateArr(arr, shiftBy)    {
    while (shift) {
        var temp = arr[arr.length - 1];
        for (var i = 0; i < array.length-1; i++) {
            array[i] = arr[i - 1];
        }
        shift--;
        arr[0] = temp;
    }
}
